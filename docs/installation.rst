============
Installation
============

At the command line::

    $ easy_install django-git

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-git
    $ pip install django-git
